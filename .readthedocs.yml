version: 2
conda:
   environment: environment.yml
python:
   install:
      - requirements: docs/requirements.txt
      - method: pip
        path: .
sphinx:
  builder: singlehtml
  configuration: docs/conf.p
